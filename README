Global setup:

  git config --global user.email sharma.animesh@gmail.com
      

Next steps:

  mkdir cqncer
  cd cqncer
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:animesh/cqncer.git
  git push origin master
      

Existing Git Repo?

  cd existing_git_repo
  git remote add origin git@github.com:animesh/cqncer.git
  git push origin master
      
